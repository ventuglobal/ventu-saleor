# Dependencias del emisor real (paquetes vendorizados de Ventu 1.0).
# facturador_v2: SII portal (requests + Playwright para el login anti-bot) + pypdf.
# boleta_bot:    API de boletas firmada con AWS SigV4 (botocore).
requests==2.32.3
python-dotenv==1.0.1
botocore==1.35.99
boto3==1.35.99
pypdf==5.1.0
playwright==1.49.1
